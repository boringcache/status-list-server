{{/*
Fallback Kubernetes Secret, rendered only when BOTH externalSecret.enabled=false AND
statuslist.fallbackSecret.enabled=true (the chart fails if both delivery modes are on).
It reconciles two keys, database-password and postgres-password: each is mirrored from the
other when omitted, and the render fails if both are set to conflicting values. When both
are empty it uses a plain `lookup "v1" "Secret" <namespace> "statuslist-secret"` by name to
reuse the existing value across upgrades when the cluster is reachable; only these two keys
and the generated certificate/signing key are reused, not arbitrary stringData. GitOps
renderers (Argo CD / Flux) cannot look up, so empty keys regenerate on each sync and should
be overridden explicitly. NOTE: this Secret carries the annotation helm.sh/resource-policy:
keep, so it SURVIVES `helm uninstall` - a reinstall against the same namespace re-authenticates
against the old password, but operators who expect uninstall to clean up may be surprised.
*/}}
{{- if and (not .Values.externalSecret.enabled) .Values.statuslist.fallbackSecret.enabled }}
{{- $stringData := dict }}
{{- range $key, $value := .Values.statuslist.fallbackSecret.stringData }}
{{- $_ := set $stringData $key $value }}
{{- end }}
{{- $db := index $stringData "database-password" | default "" }}
{{- $pg := index $stringData "postgres-password" | default "" }}
{{- if and $db $pg (ne $db $pg) }}
{{- fail "statuslist.fallbackSecret.stringData.database-password and postgres-password must match when both are set" }}
{{- end }}
{{- if and (empty $db) (empty $pg) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace "statuslist-secret" }}
{{- $existingDb := "" }}
{{- $existingPg := "" }}
{{- if and $existing $existing.data }}
{{- $existingDb = index $existing.data "database-password" | default "" }}
{{- $existingPg = index $existing.data "postgres-password" | default "" }}
{{- end }}
{{- if and $existingDb $existingPg (ne $existingDb $existingPg) }}
{{- fail "existing statuslist-secret database-password and postgres-password values differ; rotate them to the same value or set statuslist.fallbackSecret.stringData explicitly before upgrading" }}
{{- end }}
{{- if $existingDb }}
{{- $db = b64dec $existingDb }}
{{- end }}
{{- if $existingPg }}
{{- $pg = b64dec $existingPg }}
{{- else }}
{{- if empty $db }}
{{- /* GitOps renderers such as Argo CD and Flux cannot use lookup here; set an explicit database-password or disable fallbackSecret in GitOps deployments. */}}
{{- $pg = randAlphaNum 32 }}
{{- end }}
{{- end }}
{{- end }}
{{- if $db }}
{{- $pg = $db }}
{{- else }}
{{- $db = $pg }}
{{- end }}
{{- $_ := set $stringData "database-password" $db }}
{{- $_ := set $stringData "postgres-password" $pg }}
{{- $generatedCert := .Values.statuslist.fallbackSecret.generatedCertificate | default dict }}
{{- if $generatedCert.enabled }}
{{- $certKey := $generatedCert.certificateKey | default "statuslist.crt" }}
{{- $signingKey := $generatedCert.signingKey | default "statuslist.key" }}
{{- if or (empty (index $stringData $certKey)) (empty (index $stringData $signingKey)) }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace "statuslist-secret" }}
{{- $existingCert := "" }}
{{- $existingKey := "" }}
{{- if and $existing $existing.data }}
{{- $existingCert = index $existing.data $certKey | default "" }}
{{- $existingKey = index $existing.data $signingKey | default "" }}
{{- end }}
{{- if and $existingCert $existingKey }}
{{- if empty (index $stringData $certKey) }}
{{- $_ := set $stringData $certKey (b64dec $existingCert) }}
{{- end }}
{{- if empty (index $stringData $signingKey) }}
{{- $_ := set $stringData $signingKey (b64dec $existingKey) }}
{{- end }}
{{- else }}
{{- /* GitOps renderers cannot use lookup here; use explicit certificate files or disable fallbackSecret in GitOps deployments. */}}
{{- $localCert := genSelfSignedCert ($generatedCert.commonName | default "localhost") ($generatedCert.ipAddresses | default list) ($generatedCert.dnsNames | default list) 3650 }}
{{- if empty (index $stringData $certKey) }}
{{- $_ := set $stringData $certKey $localCert.Cert }}
{{- end }}
{{- if empty (index $stringData $signingKey) }}
{{- $_ := set $stringData $signingKey $localCert.Key }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  # Single supported fallback secret name. PostgreSQL (postgres.auth.existingSecret) and
  # the application's database-credentials file mount (via statuslist.secretMounts) both
  # reference this same name, so it is not independently configurable.
  name: statuslist-secret
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/resource-policy": keep
type: Opaque
stringData:
{{- range $key, $value := $stringData }}
  {{ $key }}: {{ $value | quote }}
{{- end }}
{{- end }}
