{{- if .Values.externalSecret.enabled }}
{{- /* ClusterSecretStore is cluster-scoped and managed by cluster admins, not rendered by this chart */}}
{{- $isClusterStore := eq (.Values.externalSecret.spec.secretStoreRef.kind | default "SecretStore") "ClusterSecretStore" }}
{{- if and (not .Values.secretStore.enabled) (not $isClusterStore) }}
{{- fail "externalSecret.enabled requires secretStore.enabled: no SecretStore would be rendered, leaving the ExternalSecret with a dangling secretStoreRef" }}
{{- end }}
{{- if ne .Values.externalSecret.spec.target.name (include "status-list-server-chart.appSecretName" .) }}
{{- fail (printf "externalSecret.spec.target.name must be '%s' to match the single application-secret name used by the Deployment and database subcharts" (include "status-list-server-chart.appSecretName" .)) }}
{{- end }}
{{- $appSecretName := include "status-list-server-chart.appSecretName" . }}
{{- $esoKeys := dict }}
{{- $targetTemplate := .Values.externalSecret.spec.target.template | default dict }}
{{- $hasTargetTemplate := not (empty $targetTemplate) }}
{{- $hasTemplateFrom := not (empty $targetTemplate.templateFrom) }}
{{- $mergePolicy := $targetTemplate.mergePolicy | default "Replace" }}
{{- if or (not $hasTargetTemplate) (eq $mergePolicy "Merge") }}
{{- range .Values.externalSecret.spec.data }}
{{- $_ := set $esoKeys .secretKey true }}
{{- end }}
{{- end }}
{{- with $targetTemplate.data }}
{{- range $key, $_ := . }}
{{- $_ := set $esoKeys $key true }}
{{- end }}
{{- end }}
{{- if not $hasTemplateFrom }}
{{- range $mount := .Values.statuslist.secretMounts }}
{{- if eq $mount.secretName $appSecretName }}
{{- range $item := ($mount.items | default list) }}
{{- $key := $item.key | default (include "status-list-server-chart.databasePasswordSecretKey" $) }}
{{- if not (hasKey $esoKeys $key) }}
{{- fail (printf "externalSecret.spec.data or externalSecret.spec.target.template.data must emit key %q because statuslist.secretMounts[%s] mounts it from %s" $key $mount.name $appSecretName) }}
{{- end }}
{{- end }}
{{- end }}
{{- end }}
{{- if and .Values.postgres.enabled (eq .Values.postgres.auth.existingSecret $appSecretName) }}
{{- $postgresPasswordKey := .Values.postgres.auth.secretKeys.passwordKey | default "postgres-password" }}
{{- if not (hasKey $esoKeys $postgresPasswordKey) }}
{{- fail (printf "externalSecret.spec.data or externalSecret.spec.target.template.data must emit key %q because postgres.auth.secretKeys.passwordKey uses it from %s" $postgresPasswordKey $appSecretName) }}
{{- end }}
{{- end }}
{{- end }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecret.metadata.name }}
  labels:
    {{- include "status-list-server-chart.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: {{ .Values.externalSecret.spec.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecret.spec.secretStoreRef.name }}
    kind: {{ .Values.externalSecret.spec.secretStoreRef.kind | default "SecretStore" }}
  target:
    name: {{ include "status-list-server-chart.appSecretName" . }}
    creationPolicy: {{ .Values.externalSecret.spec.target.creationPolicy }}
    {{- with .Values.externalSecret.spec.target.template }}
    template:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  data:
  {{- range .Values.externalSecret.spec.data }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        {{- toYaml .remoteRef | nindent 8 }}
  {{- end }}
{{- end }}
{{- /*
  Second ExternalSecret that provisions "aws-credentials-secret" - the exact Secret referenced by
  the Deployment's credential volume (deployment.yaml) when statuslist.aws.mountCredentials=true.
  Rendered only in ESO mode (externalSecret.enabled) so the static mounted-credentials path is
  complete and a first release does not produce a Pod that can never mount its credential volume.
*/}}
{{- if and .Values.externalSecret.enabled .Values.statuslist.aws.mountCredentials }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .Values.externalSecret.metadata.name }}-aws-credentials
  labels:
    {{- include "status-list-server-chart.labels" . | nindent 4 }}
  namespace: {{ .Release.Namespace }}
spec:
  refreshInterval: {{ .Values.externalSecret.spec.refreshInterval }}
  secretStoreRef:
    name: {{ .Values.externalSecret.spec.secretStoreRef.name }}
    kind: {{ .Values.externalSecret.spec.secretStoreRef.kind | default "SecretStore" }}
  target:
    # Fixed name - the Deployment volume references "aws-credentials-secret" directly.
    name: aws-credentials-secret
    creationPolicy: {{ .Values.externalSecret.spec.target.creationPolicy }}
    {{- with .Values.statuslist.aws.credentialsSecret.targetTemplate }}
    template:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  data:
    - secretKey: credentials
      remoteRef:
        key: {{ .Values.statuslist.aws.credentialsSecret.remoteKey }}
        property: {{ .Values.statuslist.aws.credentialsSecret.credentialsProperty }}
    - secretKey: config
      remoteRef:
        key: {{ .Values.statuslist.aws.credentialsSecret.remoteKey }}
        property: {{ .Values.statuslist.aws.credentialsSecret.configProperty }}
{{- end }}
{{- if and .Values.externalSecret.enabled .Values.externalSecret.spec.extraExternalSecrets }}
{{- range .Values.externalSecret.spec.extraExternalSecrets }}
{{- $extraStoreRef := .spec.secretStoreRef | default dict }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ .metadata.name }}
  labels:
    {{- include "status-list-server-chart.labels" $ | nindent 4 }}
    {{- with .metadata.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  namespace: {{ $.Release.Namespace }}
  {{- with .metadata.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  refreshInterval: {{ .spec.refreshInterval | default $.Values.externalSecret.spec.refreshInterval }}
  secretStoreRef:
    name: {{ $extraStoreRef.name | default $.Values.externalSecret.spec.secretStoreRef.name }}
    kind: {{ $extraStoreRef.kind | default ($.Values.externalSecret.spec.secretStoreRef.kind | default "SecretStore") }}
  target:
    name: {{ .spec.target.name }}
    creationPolicy: {{ .spec.target.creationPolicy | default $.Values.externalSecret.spec.target.creationPolicy }}
    {{- with .spec.target.template }}
    template:
      {{- toYaml . | nindent 6 }}
    {{- end }}
  {{- with .spec.data }}
  data:
    {{- range . }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        {{- toYaml .remoteRef | nindent 8 }}
    {{- end }}
  {{- end }}
  {{- with .spec.dataFrom }}
  dataFrom:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}
